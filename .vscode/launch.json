{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Lua Debug",
            "type": "lua",
            "request": "launch",
            "program": "${file}",
            "cwd": "${workspaceFolder}/artifact/scripts",
            "stopOnEntry": true,
            "console": "integratedTerminal",
        }
    ]
}