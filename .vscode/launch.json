{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "lua-local",
        "request": "launch",
        "name": "Release",
        "cwd": "${fileDirname}",
        "env": {
          "LUA_PATH": "${workspaceFolder}/artifact/scripts/?.lua;;"
        }
      }
    ]
}